package chapter2
/*
data class Book(val title:String="", val author:String="", var price:Int=0)

fun main(args:Array<String>) {
    val book = Book("Kotlin", "Daniel", 10000)
    println("${book.title} / ${book.author} / ${book.price}")

    val book2 = book.copy(price = 20000)
    println("${book2.title} / ${book2.author} / ${book2.price}")

    println(book == book2)

    book2.price = 30000
    println("${book2.title} / ${book2.author} / ${book2.price}")
}*/